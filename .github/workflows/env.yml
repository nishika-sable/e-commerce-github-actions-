name: show env variables 

on: 
 workflow_dispatch:

env:
  NAME: Nishika 

jobs: 
   show_env:
     runs-on: ubuntu-latest 
     steps: 
       - name: show plain text 
         run: echo "my name is ${{env.NAME}}"
      
   
  
    
